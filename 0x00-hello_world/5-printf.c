#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	/*prints a statement using printf*/
	printf("with proper grammar, but the outcome isa piece of art,\n");
	return (0);
}
